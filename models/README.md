Model weights are not distributed with this repository.

The default pipeline downloads `FacebookAI/roberta-base` revision `e2da8e2f811d1448a5b465c236feacd80ffbac7b` from https://huggingface.co/FacebookAI/roberta-base through the Hugging Face library. For offline execution, download that complete revision into a local directory and pass the directory with `--model-path`.
